services:
  web:
    build: .
    image: amieshonneli/compose-web:latest # Map the host computer's port 5000 to port 5000 inside the web container
    ports:
      - "5000:5000"
    depends_on:
      - redis

  redis:
    image: redis:alpine